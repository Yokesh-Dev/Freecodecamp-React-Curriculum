Build a Color Picker App


** start of index.html **

<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8" />
    <title>Color Picker</title>
    <link rel="stylesheet" href="styles.css" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/react/18.3.1/umd/react.development.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/react-dom/18.3.1/umd/react-dom.development.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/babel-standalone/7.26.5/babel.min.js"></script>
    <script
      data-plugins="transform-modules-umd"
      type="text/babel"
      src="index.jsx"
    ></script>
</head>

<body>
    <div id="root"></div>
    <script
      data-plugins="transform-modules-umd"
      type="text/babel"
      data-presets="react"
      data-type="module"
    >
      import { ColorPicker } from './index.jsx';
      ReactDOM.createRoot(document.getElementById('root')).render(<ColorPicker />);
    </script>
</body>

</html>

** end of index.html **

** start of styles.css **

body,
html {
    margin: 0;
    padding: 0;
    height: 100%;
    font-family: Arial, sans-serif;
}

#color-picker-container {
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    background-color: #ffffff;
}

input[type="color"] {
    position: absolute;
    margin-top: 50px;
    height: 40px;
}

** end of styles.css **

** start of index.jsx **

const { useState } = React;

export const ColorPicker = () => {
  const [color, setColor] = useState("#ffffff");

  function handleChange(e) {
    setColor(e.target.value);
  }

  return (
    <div id="color-picker-container" style={{ backgroundColor: color }}>
      <input
        id="color-input"
        type="color"
        value={color}
        onChange={handleChange}
      />
    </div>
  );
};


** end of index.jsx **





Explanation:


# ✅ Full Explanation of the Code

### **1. Importing `useState` from React**

```js
const { useState } = React;
```

* `useState` is a **React Hook**.
* Hooks let functional components store and update data over time.
* Here, `useState` is taken from the global React object (used in environments like CodePen or online React testers).

---

# ⭐ **2. Creating a Functional Component**

```js
export const ColorPicker = () => {
```

* A **functional component** is just a JavaScript function that returns UI (JSX).
* This component will display a color picker and a box that changes based on selected color.

---

# ⭐ **3. Using `useState` to Store Color**

```js
const [color, setColor] = useState("#ffffff");
```

This line does 3 things:

### ✔ `color`

* This is a **state variable**.
* It holds the current selected color (initial value is `"#ffffff"` — white).

### ✔ `setColor`

* This is the **function used to update** the `color` state.

### ✔ `useState("#ffffff")`

* Initializes the state with the default color.

### 🎯 Why use `useState`?

Because React re-renders the UI **whenever state changes**.
When a user picks a new color, `setColor()` changes the value and React updates the screen automatically.

---

# ⭐ **4. Event Handler for Color Change**

```js
function handleChange(e) {
  setColor(e.target.value);
}
```

### ✔ What this function does:

* It receives an event object `e` (which includes info about the user's interaction).
* `e.target` is the `<input>` field.
* `e.target.value` is the selected color value (like `"#ff0000"`).
* `setColor(...)` updates the component’s state with the new color.

### 🎯 Result:

Changing the color input → updates state → React re-renders → background color changes.

---

# ⭐ **5. Rendering the UI**

```jsx
return (
  <div id="color-picker-container" style={{ backgroundColor: color }}>
```

### ✔ This `<div>` is the background container.

* Its background color is controlled by the `color` state.
* When `setColor` updates the color, this background automatically changes.

---

# ⭐ **6. The Color Input**

```jsx
<input 
  onChange={handleChange}
  id="color-input"
  type="color" 
  color={color}
/>
```

### Let’s break down each attribute:

#### **📌 `id="color-input"`**

* Gives the input a unique identifier (used by tests or CSS).

#### **📌 `type="color"`**

* Creates a color picker UI provided by the browser.

#### **📌 `onChange={handleChange}`**

* Runs `handleChange` whenever the user picks a new color.
* This updates state and refreshes the UI.

#### ⚠ **Note:**

`color={color}` is not correct — it should be `value={color}` in a real React setup.

---

# ⭐ **7. How React Updates the View**

React's update cycle:

1. User picks a color
2. `handleChange()` runs
3. `setColor(newColor)` updates state
4. React re-renders the component
5. New `color` value updates:

   * The background color of the container
   * The color input’s value (if using `value={color}`)

This is called **React’s one-way data flow**.

---

# 🔥 Key Concepts Used in This Code

| Concept                  | Explanation                                             |
| ------------------------ | ------------------------------------------------------- |
| **Functional Component** | A function that returns UI (JSX).                       |
| **JSX**                  | HTML-like syntax inside JavaScript.                     |
| **useState Hook**        | Stores and updates state inside functional components.  |
| **State**                | Data that changes over time and triggers UI updates.    |
| **Event Handling**       | Functions that respond to user actions (like onChange). |
| **Controlled Component** | The input’s value is controlled by React state.         |
| **Re-rendering**         | React updates the UI when state changes.                |

---

# 🎉 Summary (Simplified)

* The component displays a color picker.
* It stores the selected color using `useState`.
* Whenever the input changes, the state updates.
* React re-renders the component.
* The background color updates to match the selected color.



